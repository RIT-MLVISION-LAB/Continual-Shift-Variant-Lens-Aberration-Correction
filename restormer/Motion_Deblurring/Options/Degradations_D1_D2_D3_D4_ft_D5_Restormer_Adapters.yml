## D5 (Low-light Enhancement) with D1 (Deblurring Variant 1) Pretrained Weights -> D2 (Denoising) -> D3 (Dehazing) -> D4 (Deraining) Adapters
# general settings
name: Degradations_D1_D2_D3_D4_ft_D5_Restormer_Adapters
model_type: AdapterModel
scale: 1
num_gpu: 4 # set to 0 for cpu mode, adjust based on available GPUs
manual_seed: 100

# adapter settings
adapter_training: true
commit_loaded_adapter: true  # false for D2, true for D3 onward
current_domain: 3  # D1=-1 (pure backbone), D2=0, D3=1, D4=2, D5=3

# dataset and data loader settings
datasets:
  train:
    name: TrainSet_Degradations_D1_D2_D3_D4_ft_D5_Adapters
    type: Dataset_PairedImage
    dataroot_gt: ./Motion_Deblurring/Datasets/train/D5_lowlight/target_crops
    dataroot_lq: ./Motion_Deblurring/Datasets/train/D5_lowlight/input_crops
    geometric_augs: true

    filename_tmpl: "{}"
    io_backend:
      type: disk

    # data loader
    use_shuffle: true
    num_worker_per_gpu: 8
    batch_size_per_gpu: 8

    mini_batch_sizes: [8]  # options: [8], [8,6,4,2] (for progressive training) (High priority)
    iters: [50000] # options: [50000], [100000], [200000], [40000, 20000, 20000, 20000] (for progressive training with 100k iterations) (High priority)
    gt_size: 224 # options: 128, 224 (High priority)
    gt_sizes: [224] # options: [128], [128,160,192,224] (for progressive training) (High priority)

    dataset_enlarge_ratio: 1
    prefetch_mode: ~

  val:
    name: ValSet_Degradations_D1_D2_D3_D4_ft_D5_Adapters
    type: Dataset_PairedImage
    dataroot_gt: ./Motion_Deblurring/Datasets/val/D5_lowlight/target_crops
    dataroot_lq: ./Motion_Deblurring/Datasets/val/D5_lowlight/input_crops
    io_backend:
      type: disk

# network structures
network_g:
  type: RestormerAdapters
  inp_channels: 3
  out_channels: 3
  dim: 48
  num_blocks: [4, 6, 6, 8]
  num_refinement_blocks: 4
  heads: [1, 2, 4, 8]
  ffn_expansion_factor: 2.66
  bias: False
  LayerNorm_type: WithBias
  # adapter configuration
  adapter_config:
    bottleneck: 64  # options: 32, 64, 128 (Medium priority)
    dropout: 0.1  # options: 0.0, 0.1, 0.2 (Low priority)
    adapter_scalar: "1.0"  # options: "learnable_scalar", "0.5", "2.0" (Medium priority)
    adapter_layernorm_option: in  # options: out, None (Low priority)
    adapter_option: sequential  # options: parallel, sequential (High priority)
    adapter_momentum: 0.0

# path
path:
  pretrain_network_g: ./experiments/archived_checkpoints/Degradations_D1_D2_D3_ft_D4_Adapters/net_g_latest.pth
  strict_load_g: false # allows loading checkpoints with missing keys (extra adapter params randomly initialized)
  resume_state: ~

# training settings
train:
  total_iter: 50000  # options: 50000, 100000, 200000 (High priority)
  warmup_iter: -1 # options: -1 (no warm up), 2000 (Low priority)
  use_grad_clip: true

  scheduler:
    type: CosineAnnealingRestartCyclicLR
    periods: [50000]  # options: [50000], [50000, 50000] (for 100k iters), [100000, 100000] (for 200k iters) (Low priority)
    restart_weights: [1]  # options: [1], [1, 1] (for multiple cycles) (Low priority)
    eta_mins: [0.0003]  # options: [0.0003], [0.000001], [0.0003, 0.000001] (for multiple cycles) (Low priority)

  mixing_augs:
    mixup: false
    mixup_beta: 1.2
    use_identity: true

  optim_g:
    type: AdamW
    lr: !!float 2e-4  # options: 1e-4, 2e-4, 3e-4 (Medium priority)
    weight_decay: !!float 1e-4
    betas: [0.9, 0.999]

  # losses
  pixel_opt:
    type: L1Loss
    loss_weight: 1
    reduction: mean

# validation settings
val:
  window_size: 8
  val_freq: !!float 2e3
  save_img: false
  rgb2bgr: true
  use_image: true
  max_minibatch: 8

  metrics:
    psnr:
      type: calculate_psnr
      crop_border: 0
      test_y_channel: false
    ssim:
      type: calculate_ssim
      crop_border: 0
      test_y_channel: false

# logging settings
logger:
  print_freq: 1000
  save_checkpoint_freq: !!float 5e3
  use_tb_logger: true
  wandb:
    project: ~
    resume_id: ~

# dist training settings
dist_params:
  backend: nccl
  port: 29500
